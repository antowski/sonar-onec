package org.antowski.onec.parser.grammar.declaration;


import org.antowski.onec.ast.api.OneCTokenType;
import org.antowski.onec.parser.OneCLexer;
import org.junit.Test;

import static org.antowski.onec.utils.Assertions.assertThat;

public class VariableDeclarationTest {

    @Test
    public void realLife() {
        assertThat(OneCLexer.SPACING)
    }

}
